<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>My Vehicle</title>



<style>



  body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 20px;
    background-image: url('https://tigerelectric.com/wp-content/uploads/2023/12/tiger-electric-ev-charging-levels-7x5-1.jpg');
    background-size: cover;
    background-position: center;
    position: relative;
    overflow: hidden;
  }

  /* 🌞 Glowing Animated Sun */
  .sun {
    position: absolute;
    top: 40px;
    right: 60px;
    width: 120px;
    height: 120px;
    background: radial-gradient(circle, #fffd8b, #ffbb00, #ff8a00);
    border-radius: 50%;
    box-shadow: 0 0 40px #ffb300, 0 0 80px #ff8a00;
    animation: sunPulse 4s infinite ease-in-out alternate;
    z-index: 0;
  }

  @keyframes sunPulse {
    0% { transform: scale(1); opacity: 0.85; }
    100% { transform: scale(1.15); opacity: 1; }
  }

  /* 🌟 Soft rotating rays */
  .sun-rays {
    position: absolute;
    top: 5px;
    right: 20px;
    width: 200px;
    height: 200px;
    background: conic-gradient(
      rgba(255,210,0,0.8),
      rgba(255,150,0,0.3),
      rgba(255,210,0,0.8)
    );
    border-radius: 50%;
    filter: blur(40px);
    opacity: 0.6;
    animation: rotateRays 12s linear infinite;
    z-index: 0;
  }

  @keyframes rotateRays { 
    100% { transform: rotate(360deg); }
  }

  /* 🌤 Glass Blur Card + EV Glow */
  .section {
    background: rgba(255, 255, 255, 0.15);
    padding: 25px;
    border-radius: 14px;
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    margin-bottom: 40px;
    box-shadow: 0 0 20px rgba(20, 255, 255, 0.4);
    animation: fadeIn 0.8s ease;
    position: relative;
    z-index: 2;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .container {
    max-width: 800px;
    margin: 0 auto;
    position: relative;
    z-index: 2;
  }

  h1 {
    text-align: center;
    color: #ffffff;
    text-shadow: 0 0 12px #00e1ff;
    margin-bottom: 25px;
    font-size: 32px;
    letter-spacing: 1px;
  }

  .vehicle-info {
    margin-bottom: 18px;
    font-size: 18px;
    color: #fff;
  }

  .info-label {
    font-weight: bold;
    color: #08fa38;
    text-shadow: 0 0 8px rgb(0, 0, 0);
  }

  /* 🚗 EV Glow Button */
  .edit-btn {
    background: linear-gradient(135deg, #00ffbb, #00cc88);
    color: white;
    border: none;
    border-radius: 8px;
    padding: 12px 24px;
    cursor: pointer;
    font-size: 17px;
    transition: 0.3s ease;
    box-shadow: 0 0 15px #00ffcc;
  }

  .edit-btn:hover {
    background: #525252;
    box-shadow: 0 0 25px #3300ff;
  }

</style>
</head>

<body>

<!-- 🌞 Solar Animations -->
<div class="sun"></div>
<div class="sun-rays"></div>

<div class="container">
  <h1>My Vehicle Details</h1>

  <div class="section">
    <div class="vehicle-info">
      <span class="info-label">Vehicle Model: </span>
      <span><%= user.veichleModelName %></span>
    </div>

    <div class="vehicle-info">
      <span class="info-label">Vehicle Number: </span>
      <span><%= user.veichleNumber %></span>
    </div>

    <div class="vehicle-info">
      <span class="info-label">Age of Vehicle: </span>
      <span><%= user.ageOfVeichle %> months</span>
    </div>

    <div class="vehicle-info">
      <span class="info-label">Driver's ID: </span>
      <span><%= user.driversId %></span>
    </div>

    <button class="edit-btn" onclick="backtohome()">BACK TO HOME</button>
  </div>
</div>

<script>
function backtohome() {
  window.location.href = "/profile";
}
</script>

</body>
</html>


























